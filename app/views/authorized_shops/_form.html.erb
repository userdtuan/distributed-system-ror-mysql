<%= form_with(model: authorized_shop) do |form| %>
  <% if authorized_shop.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(authorized_shop.errors.count, "error") %> prohibited this authorized_shop from being saved:</h2>

      <ul>
        <% authorized_shop.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :address, style: "display: block" %>
    <%= form.text_field :address %>
  </div>

  <div>
    <%= form.label :owner_name, style: "display: block" %>
    <%= form.text_field :owner_name %>
  </div>

  <div>
    <%= form.label :productid, style: "display: block" %>
    <%= form.number_field :productid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
